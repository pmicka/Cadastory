name: Farm Watch landscape structure materialization

on:
  issues:
    types: [opened]

permissions:
  contents: read
  id-token: write

jobs:
  materialize:
    if: >-
      github.actor == 'pmicka' &&
      github.event.issue.user.login == 'pmicka' &&
      github.event.issue.title == '[ops] Materialize Farm Watch landscape structure context'
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Check landscape structure materializer
        run: deno check scripts/farm-watch-landscape-structure-materialize.ts

      - name: Materialize validation property
        run: >-
          deno run
          --allow-env
          --allow-net
          scripts/farm-watch-landscape-structure-materialize.ts
          --property validation-property-01
