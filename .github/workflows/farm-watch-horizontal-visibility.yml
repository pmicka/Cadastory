name: Farm Watch horizontal visibility materialization

on:
  issues:
    types: [opened]

permissions:
  contents: read
  id-token: write

jobs:
  materialize:
    if: >-
      github.actor == 'pmicka' &&
      github.event.issue.user.login == 'pmicka' &&
      github.event.issue.title == '[ops] Materialize Farm Watch horizontal visibility'
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: denoland/setup-deno@v2
        with:
          deno-version: v2.x

      - name: Check horizontal visibility materializer
        run: deno check scripts/farm-watch-horizontal-visibility-materialize.ts

      - name: Materialize validation property
        run: >-
          deno run
          --allow-env
          --allow-net
          scripts/farm-watch-horizontal-visibility-materialize.ts
          --property validation-property-01
