// Canonical Scout component sandbox entrypoint.
// Keep the versioned server implementation separate so protocol-level changes remain
// easy to review, test, and roll back without mixing them into presentation code.
import './index_v20.ts'
