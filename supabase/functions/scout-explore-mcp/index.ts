import 'jsr:@supabase/functions-js@2.4.5/edge-runtime.d.ts'
import { createClient } from 'npm:@supabase/supabase-js@2.115.0'
import { McpServer } from 'npm:@modelcontextprotocol/sdk@1.29.0/server/mcp.js'
import { WebStandardStreamableHTTPServerTransport } from 'npm:@modelcontextprotocol/sdk@1.29.0/server/webStandardStreamableHttp.js'
import { registerAppResource, registerAppTool, RESOURCE_MIME_TYPE } from 'npm:@modelcontextprotocol/ext-apps@1.7.5/server'
import * as z from 'npm:zod@4.2.0/v4'

const SUPABASE_URL=Deno.env.get('SUPABASE_URL')!
let SERVICE_KEY=Deno.env.get('SUPABASE_SERVICE_ROLE_KEY')||''
try{SERVICE_KEY=JSON.parse(Deno.env.get('SUPABASE_SECRET_KEYS')||'{}').default||SERVICE_KEY}catch{}
if(!SERVICE_KEY)throw new Error('Scout exploration database secret unavailable')
const db=createClient(SUPABASE_URL,SERVICE_KEY,{auth:{persistSession:false,autoRefreshToken:false}})
const CONTRACT_VERSION='privacy-contract-v2'
const EXPOSURE_CONTRACT_VERSION='scout-exposure-v1'
const ENUMERATION_CONTRACT_VERSION='scout-enumeration-v1'
const LENS_URI='ui://scout/lens/v1'
const TIME_URI='ui://scout/evidence-time-machine/v1'
const CONSTELLATION_URI='ui://scout/constellations/v1'

type Connection={connection_id:string;organization_id?:string|null;scopes:string[];expires_at?:string|null}
type SafeToolError={code:string;message:string;details:string|null;hint:string|null;validation_reason:string|null;retryable:boolean;diagnostic_id:string}
async function authenticate(req:Request):Promise<Connection|null>{const m=(req.headers.get('authorization')||'').match(/^Bearer\s+(.+)$/i);if(!m)return null;const{data,error}=await db.rpc('scout_resolve_agent_connection_v4',{p_token:m[1].trim(),p_privacy_contract:CONTRACT_VERSION,p_exposure_contract:EXPOSURE_CONTRACT_VERSION,p_enumeration_contract:ENUMERATION_CONTRACT_VERSION});return error||!data?null:data as Connection}
function requireScope(c:Connection,s:string){if(!Array.isArray(c.scopes)||!c.scopes.includes(s))throw new Error(`connection does not grant ${s}`)}
async function rpc(name:string,args:Record<string,unknown>){const{data,error}=await db.rpc(name,args);if(error)throw error;return data}
function safeText(v:unknown,max=2000):string|null{if(v===null||v===undefined)return null;let s='';if(typeof v==='string')s=v;else if(typeof v==='number'||typeof v==='boolean')s=String(v);else{try{s=JSON.stringify(v)}catch{s=''}}s=s.trim();return s?s.slice(0,max):null}
function normalizeToolError(e:unknown):SafeToolError{const o=(e&&typeof e==='object')?e as Record<string,unknown>:{};const code=safeText(o.code??o.error_code??o.name,120)||'scout_tool_error';const message=safeText(o.message,2000)||(e instanceof Error?safeText(e.message,2000):null)||safeText(e,2000)||'Scout exploration request failed';const details=safeText(o.details??o.detail,2000);const hint=safeText(o.hint,2000);const statusRaw=o.status??o.statusCode;const status=typeof statusRaw==='number'?statusRaw:Number(statusRaw);return{code,message,details,hint,validation_reason:safeText(o.validation_reason??o.reason,2000),retryable:status===408||status===429||status>=500,diagnostic_id:crypto.randomUUID()}}
function errorResult(e:unknown){const error=normalizeToolError(e);return{isError:true,content:[{type:'text' as const,text:JSON.stringify({error},null,2)}],structuredContent:{error}}}
function result(data:any,text:string){return{content:[{type:'text' as const,text}],structuredContent:data&&typeof data==='object'&&!Array.isArray(data)?data:{result:data}}}
function addUi(data:any,surface:string,extra:Record<string,unknown>={}){return data&&typeof data==='object'&&!Array.isArray(data)?{...data,...extra,ui_surface:surface}:{result:data,...extra,ui_surface:surface}}

const candidateKey=z.string().min(3).max(300).regex(/^[A-Za-z0-9_.:-]+$/)
const siteAccess=z.enum(['access_context_unresolved','access_enrichment_pending'])
const recurrence=z.enum(['contextual','cycle_context','documented_cadence','event_driven','lifecycle_context','partial_cadence','project_stage','seasonal'])
const windowState=z.enum(['active_ideal_window','active_time_window','expired','not_yet_open','ongoing_context','upcoming_or_contextual_window'])
const freshness=z.enum(['current_by_explicit_window','expired_by_explicit_window','future_window','source_timestamp_present'])
const signalStrength=z.enum(['context','estimate','fair','high','medium','moderate','none','plan','plan_estimate','poor','scheduled_plan_estimate','serious','strong','weak'])
const lens=z.object({time_sensitive:z.boolean().optional(),buyer_resolved:z.boolean().optional(),contact_available:z.boolean().optional(),procurement_route_available:z.boolean().optional(),commercial_scale_documented:z.boolean().optional(),site_access_status:z.array(siteAccess).min(1).max(8).optional(),recurrence_status:z.array(recurrence).min(1).max(12).optional(),window_status:z.array(windowState).min(1).max(8).optional(),freshness_status:z.array(freshness).min(1).max(8).optional(),signal_strength:z.array(signalStrength).min(1).max(20).optional(),due_within_days:z.number().int().min(1).max(730).optional(),event_within_days:z.number().int().min(1).max(730).optional()}).strict()

const APP_HTML=String.raw`<!doctype html><html lang="en"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="color-scheme" content="light dark"><title>Scout Explore</title><style>
:root{color-scheme:light dark;--bg:light-dark(#f7f7f4,#141414);--card:light-dark(#fff,#202020);--soft:light-dark(#f0f0ec,#292929);--soft2:light-dark(#e9e9e4,#313131);--text:light-dark(#171716,#f5f5f2);--muted:light-dark(#686860,#aaa9a2);--line:light-dark(#deded7,#3d3d39);--accent:light-dark(#2854a3,#8db0ff);--fact:light-dark(#185e46,#79d2ac);--infer:light-dark(#6d4ea0,#c3a6f4);--counter:light-dark(#9a3d36,#f1a09a);--decision:light-dark(#805d00,#e8c46f);--radius:18px;--rsm:12px;font-family:ui-sans-serif,system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}*{box-sizing:border-box}html,body{margin:0;background:transparent;color:var(--text)}body{padding:max(12px,env(safe-area-inset-top)) max(12px,env(safe-area-inset-right)) max(16px,env(safe-area-inset-bottom)) max(12px,env(safe-area-inset-left))}button,input{font:inherit}.app{max-width:760px;margin:auto;display:grid;gap:11px}.top{display:flex;justify-content:space-between;align-items:center;gap:10px}.brand{font-size:12px;font-weight:850;letter-spacing:.09em;text-transform:uppercase;color:var(--muted)}.mode{font-size:11px;border:1px solid var(--line);background:var(--soft);border-radius:999px;padding:5px 8px;color:var(--muted)}.panel{background:var(--card);border:1px solid var(--line);border-radius:var(--radius);overflow:hidden}.head{padding:16px 17px 12px;display:grid;gap:7px}.kicker{font-size:11px;font-weight:850;letter-spacing:.08em;text-transform:uppercase;color:var(--muted)}h1{font-size:clamp(21px,4.5vw,29px);line-height:1.08;letter-spacing:-.025em;margin:0}.sub{color:var(--muted);font-size:13px;line-height:1.4}.toolbar{display:flex;gap:7px;align-items:center;flex-wrap:wrap}.btn,.chip,.nodeBtn,.railBtn{min-height:44px;border:1px solid var(--line);background:var(--card);color:var(--text);border-radius:11px;padding:8px 11px;cursor:pointer;font-weight:720}.btn.primary{background:var(--text);border-color:var(--text);color:var(--bg)}.btn.quiet{background:var(--soft)}.btn:disabled{opacity:.45;cursor:default}.btn:focus-visible,.chip:focus-visible,.nodeBtn:focus-visible,.railBtn:focus-visible,input:focus-visible{outline:3px solid currentColor;outline-offset:2px}.chips{display:flex;gap:7px;flex-wrap:wrap}.chip{min-height:36px;border-radius:999px;padding:6px 9px;font-size:12px;background:var(--soft)}.chip.on{background:var(--text);color:var(--bg);border-color:var(--text)}.chip.no{border-style:dashed}.summaryStrip{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px;padding:0 17px 15px}.metric{background:var(--soft);border-radius:var(--rsm);padding:10px;min-width:0}.metric b{display:block;font-size:11px;color:var(--muted);text-transform:uppercase;letter-spacing:.055em;margin-bottom:3px}.metric span{font-size:13px;line-height:1.3}.section{border-top:1px solid var(--line);padding:14px 17px;display:grid;gap:10px}.sectionTitle{display:flex;justify-content:space-between;align-items:center;gap:8px}.sectionTitle b{font-size:13px}.sectionTitle span{font-size:12px;color:var(--muted)}.editor{display:none;gap:12px}.editor.open{display:grid}.filterGroup{display:grid;gap:7px}.filterLabel{font-size:11px;font-weight:800;text-transform:uppercase;letter-spacing:.055em;color:var(--muted)}.enumWrap{display:flex;gap:6px;flex-wrap:wrap}.numRow{display:flex;align-items:center;gap:8px}.numRow input{width:100px;min-height:44px;border:1px solid var(--line);border-radius:10px;background:var(--card);color:var(--text);padding:8px}.carouselWrap{position:relative}.carousel{display:flex;gap:10px;overflow-x:auto;scroll-snap-type:x mandatory;padding:2px 2px 8px;scrollbar-width:none}.carousel::-webkit-scrollbar{display:none}.candidate{scroll-snap-align:start;flex:0 0 min(88%,360px);border:1px solid var(--line);background:var(--card);border-radius:15px;padding:14px;display:grid;gap:10px;cursor:pointer}.candidate.selected{border-color:var(--text);box-shadow:0 0 0 1px var(--text) inset}.candTop{display:flex;justify-content:space-between;gap:9px;align-items:flex-start}.candName{font-weight:820;line-height:1.22}.tag{font-size:10px;border:1px solid var(--line);border-radius:999px;padding:4px 6px;color:var(--muted);white-space:nowrap}.why{font-size:13px;line-height:1.42}.mini{display:grid;grid-template-columns:1fr 1fr;gap:7px}.mini div{background:var(--soft);border-radius:10px;padding:8px;min-width:0}.mini b{display:block;font-size:10px;color:var(--muted);text-transform:uppercase;margin-bottom:3px}.mini span{font-size:12px}.railControls{display:flex;justify-content:space-between;gap:8px;align-items:center}.railControls .left,.railControls .right{display:flex;gap:7px}.railViewport{overflow-x:auto;scrollbar-width:none;padding:8px 3px}.railViewport::-webkit-scrollbar{display:none}.railTrack{min-width:max-content;display:flex;align-items:flex-start;position:relative;padding:22px 8px 6px;gap:12px}.railTrack:before{content:"";position:absolute;left:8px;right:8px;top:38px;height:2px;background:var(--line)}.eventNode,.nowNode{position:relative;z-index:1;width:112px;display:grid;justify-items:center;gap:6px;text-align:center}.eventNode button{width:44px;height:44px;border-radius:50%;border:2px solid var(--line);background:var(--card);color:var(--text);font-weight:900;cursor:pointer}.eventNode.inference button{transform:rotate(45deg);border-radius:9px}.eventNode.inference button span{display:block;transform:rotate(-45deg)}.eventNode.counter_evidence button{clip-path:polygon(50% 4%,96% 94%,4% 94%);border-radius:0;padding-top:9px}.eventNode.decision button{border-radius:9px}.eventNode.future button{border-style:dashed}.eventNode.selected button{border-color:var(--text);box-shadow:0 0 0 3px color-mix(in srgb,var(--text) 12%,transparent)}.eventNode small,.nowNode small{font-size:10px;color:var(--muted);line-height:1.25}.nowNode{width:72px}.nowMark{width:3px;height:44px;background:var(--text);border-radius:9px}.eventDetail{background:var(--soft);border-radius:14px;padding:13px;display:grid;gap:7px}.eventMeta{display:flex;gap:6px;flex-wrap:wrap}.eventMeta .tag.fact{color:var(--fact)}.eventMeta .tag.inference{color:var(--infer)}.eventMeta .tag.counter_evidence{color:var(--counter)}.eventMeta .tag.decision{color:var(--decision)}.graphWrap{display:grid;gap:10px}.graph{width:100%;aspect-ratio:16/9;max-height:360px;background:var(--soft);border-radius:15px}.edge.supporting{stroke-dasharray:5 5}.edge.strong{stroke-width:4}.edge.medium{stroke-width:3}.edge{stroke:var(--muted);stroke-width:2;opacity:.65}.node{fill:var(--card);stroke:var(--text);stroke-width:2}.node.selected{fill:var(--text)}.nodeLabel{font-size:12px;font-weight:900;fill:var(--text);text-anchor:middle;dominant-baseline:middle}.nodeLabel.selected{fill:var(--bg)}.nodeList,.edgeList{display:flex;gap:7px;flex-wrap:wrap}.nodeBtn{min-height:40px;border-radius:999px;padding:7px 10px;font-size:12px;background:var(--soft)}.nodeBtn.on{background:var(--text);color:var(--bg)}.edgeCard{background:var(--soft);border-radius:11px;padding:9px 10px;font-size:12px;line-height:1.35}.empty,.error{border:1px solid var(--line);border-radius:var(--radius);padding:20px;background:var(--card);display:grid;gap:8px}.error{border-color:var(--counter)}.back{min-height:40px;border:0;background:transparent;color:var(--muted);padding:4px 0;cursor:pointer;text-align:left;font-weight:750}@media(max-width:500px){.summaryStrip{grid-template-columns:1fr}.mini{grid-template-columns:1fr}.graph{aspect-ratio:4/3}}@media(prefers-reduced-motion:reduce){*{scroll-behavior:auto!important;transition:none!important}} </style></head><body><main class="app"><div class="top"><div class="brand">Scout · Explore</div><div id="mode" class="mode">Loading</div></div><div id="root"><div class="empty"><b>Preparing Scout exploration…</b><span class="sub">Known-candidate analysis only.</span></div></div></main><script type="module">
import { App } from 'https://esm.sh/@modelcontextprotocol/ext-apps@1.7.5?bundle';
const app=new App({name:'Scout Explore',version:'1.0.0'});let state=null,catalog=null,history=[],selected=0,timelineFilter='all',editorLens={};const root=document.getElementById('root'),mode=document.getElementById('mode');
const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const label=s=>String(s||'').replaceAll('_',' ').replaceAll('-',' ').replace(/\b\w/g,m=>m.toUpperCase());const norm=r=>r?.structuredContent??r??null;const obj=v=>v&&typeof v==='object'&&!Array.isArray(v)?v:{};const arr=v=>Array.isArray(v)?v:[];const date=v=>{if(!v)return'Unknown time';const d=new Date(v);return Number.isNaN(d.getTime())?String(v):new Intl.DateTimeFormat(undefined,{month:'short',day:'numeric',year:'numeric',hour:'numeric',minute:'2-digit'}).format(d)};
function detect(s){if(s?.ui_surface)return s.ui_surface;if(Array.isArray(s?.dimensions))return'lens_catalog';if(Array.isArray(s?.events))return'time_machine';if(Array.isArray(s?.nodes)&&Array.isArray(s?.edges))return'constellations';if(Array.isArray(s?.results)&&s?.lens!==undefined)return'lens';return'unknown'}
function backButton(){return history.length?'<button id="back" class="back">← Back</button>':''}function wireBack(){const b=document.getElementById('back');if(b)b.onclick=()=>{state=history.pop();selected=0;render()}}
async function go(name,args){const prev=state;try{const r=await app.callServerTool({name,arguments:args});if(r.isError)throw new Error(r.content?.[0]?.text||'Scout request failed');history.push(prev);state=norm(r);selected=0;render()}catch(e){root.innerHTML='<div class="error"><b>Couldn’t update this Scout view</b><span class="sub">'+esc(e?.message||'Unknown error')+'</span></div>'}}
function metric(k,v){return '<div class="metric"><b>'+esc(k)+'</b><span>'+esc(v??'Unknown')+'</span></div>'}
function render(){if(!state){return}const kind=detect(state);mode.textContent=kind==='lens_catalog'?'Scout Lens':kind==='lens'?'Scout Lens':kind==='time_machine'?'Evidence Time Machine':kind==='constellations'?'Opportunity Constellations':'Scout Explore';if(state.allowed===false){root.innerHTML='<div class="empty">'+backButton()+'<b>That candidate set is unavailable</b><span class="sub">'+esc(state.reason||state.guardrail||'Scout cannot expose this result.')+'</span></div>';wireBack();return}if(kind==='lens_catalog')renderCatalog();else if(kind==='lens')renderLens();else if(kind==='time_machine')renderTimeline();else if(kind==='constellations')renderConstellation();else root.innerHTML='<div class="empty"><b>No exploration surface for this result</b></div>'}
function renderCatalog(){const dims=arr(state.dimensions);root.innerHTML='<section class="panel"><div class="head">'+backButton()+'<div class="kicker">Semantic filters</div><h1>Scout Lens</h1><div class="sub">These filters narrow opportunities Scout has already shown you. They are not ranking weights and cannot discover hidden leads.</div></div><div class="section"><div class="chips">'+dims.map(d=>'<span class="chip">'+esc(d.title||label(d.key))+'</span>').join('')+'</div></div><div class="section"><div class="sub">Ask Scout to apply a lens after opportunity discovery, or use the controls when Lens opens on a known candidate set.</div></div></section>';wireBack()}
function activeLensText(l){const out=[];for(const [k,v] of Object.entries(obj(l))){if(Array.isArray(v)&&v.length)out.push(label(k)+': '+v.map(label).join(', '));else if(typeof v==='boolean')out.push(label(k)+': '+(v?'yes':'no'));else if(v!==null&&v!==undefined&&v!=='')out.push(label(k)+': '+v)}return out}
function candidateCard(r,i){const o=obj(r.opportunity),t=obj(o.target),sig=obj(o.signal),buyer=obj(o.buyer),timing=obj(o.timing),scale=obj(o.commercial_scale),loc=obj(o.location);return '<article class="candidate '+(i===selected?'selected':'')+'" data-cand="'+i+'" tabindex="0"><div class="candTop"><div class="candName">'+esc(o.display_name||t.name||r.candidate_key)+'</div><span class="tag">'+esc(label(o.primary_service_slug||arr(o.services)[0]||'Opportunity'))+'</span></div><div class="why">'+esc(sig.why_now||'Scout has current evidence worth investigating.')+'</div><div class="mini"><div><b>Buyer</b><span>'+esc(buyer.organization_name||label(buyer.resolution_status||'unresolved'))+'</span></div><div><b>Timing</b><span>'+esc(label(timing.window_status||'unknown'))+'</span></div><div><b>Scale</b><span>'+esc(label(scale.status||'unknown'))+'</span></div><div><b>Location</b><span>'+esc([loc.county_name,loc.state_code].filter(Boolean).join(', ')||'Unknown')+'</span></div></div></article>'}
async function ensureCatalog(){if(catalog)return catalog;const r=await app.callServerTool({name:'scout_get_opportunity_lens_catalog',arguments:{}});if(!r.isError)catalog=norm(r);return catalog}
function renderLens(){const results=arr(state.results),base=arr(state.candidate_keys),active=activeLensText(state.lens);if(selected>=results.length)selected=Math.max(0,results.length-1);root.innerHTML='<section class="panel"><div class="head">'+backButton()+'<div class="kicker">Known-candidate filter</div><h1>Scout Lens</h1><div class="sub">'+esc(state.result_count??results.length)+' of '+esc(state.requested_candidate_count??base.length)+' candidates match the current lens.</div><div class="chips">'+(active.length?active.map(x=>'<span class="chip on">'+esc(x)+'</span>').join(''):'<span class="chip">No filters</span>')+'</div><div class="toolbar"><button id="tune" class="btn quiet">Tune lens</button><button id="clearLens" class="btn" '+(!active.length?'disabled':'')+'>Clear</button></div></div><div id="editor" class="section editor"><div class="sectionTitle"><b>Semantic dimensions</b><span>Base set stays fixed</span></div><div id="controls"><span class="sub">Loading lens controls…</span></div><div class="toolbar"><button id="applyLens" class="btn primary">Apply</button></div></div><div class="section"><div class="sectionTitle"><b>Matching opportunities</b><span>'+esc(results.length)+' shown</span></div>'+(results.length?'<div class="railControls"><div class="left"><button id="prevCand" class="railBtn" aria-label="Previous opportunity">←</button><button id="nextCand" class="railBtn" aria-label="Next opportunity">→</button></div></div><div id="carousel" class="carousel">'+results.map(candidateCard).join('')+'</div>':'<div class="empty"><b>No candidates match this lens</b><span class="sub">Clear or loosen a semantic filter. Scout will not invent replacement leads.</span></div>')+'</div>'+(results.length?'<div class="section"><div class="toolbar"><button id="time" class="btn primary">Time Machine</button><button id="const" class="btn" '+(results.length<2?'disabled':'')+'>Constellation</button></div></div>':'')+'</section>';wireBack();editorLens=JSON.parse(JSON.stringify(obj(state.lens)||{}));const tune=document.getElementById('tune'),editor=document.getElementById('editor');tune.onclick=async()=>{editor.classList.toggle('open');if(editor.classList.contains('open')){const c=await ensureCatalog();renderLensControls(arr(c?.dimensions))}};document.getElementById('clearLens').onclick=()=>go('scout_apply_opportunity_lens',{candidate_keys:base,lens:{},limit:25});for(const el of document.querySelectorAll('[data-cand]')){const choose=()=>{selected=Number(el.dataset.cand||0);renderLens()};el.onclick=choose;el.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();choose()}}}const step=d=>{selected=Math.max(0,Math.min(results.length-1,selected+d));renderLens();setTimeout(()=>document.querySelector('[data-cand="'+selected+'"]')?.scrollIntoView({behavior:'smooth',inline:'center',block:'nearest'}),0)};const p=document.getElementById('prevCand'),n=document.getElementById('nextCand');if(p)p.onclick=()=>step(-1);if(n)n.onclick=()=>step(1);const time=document.getElementById('time'),con=document.getElementById('const');if(time)time.onclick=()=>go('scout_get_opportunity_timeline',{candidate_key:results[selected].candidate_key,include_future:true,limit:100});if(con)con.onclick=()=>go('scout_get_opportunity_constellation',{candidate_keys:results.slice(0,12).map(x=>x.candidate_key),max_proximity_miles:25})}
function renderLensControls(dims){const controls=document.getElementById('controls');if(!controls)return;controls.innerHTML=dims.map(d=>{const k=d.key,title=d.title||label(k),type=d.value_type,vals=arr(d.allowed_values);if(type==='boolean'){const cur=editorLens[k];const text=cur===true?'Yes':cur===false?'No':'Any';return '<div class="filterGroup"><div class="filterLabel">'+esc(title)+'</div><button class="chip '+(cur!==undefined?'on':'')+'" data-bool="'+esc(k)+'">'+esc(text)+'</button></div>'}if(type==='enum_set')return '<div class="filterGroup"><div class="filterLabel">'+esc(title)+'</div><div class="enumWrap">'+vals.map(v=>'<button class="chip '+(arr(editorLens[k]).includes(v)?'on':'')+'" data-enum="'+esc(k)+'" data-val="'+esc(v)+'">'+esc(label(v))+'</button>').join('')+'</div></div>';if(type==='integer')return '<div class="filterGroup"><div class="filterLabel">'+esc(title)+'</div><div class="numRow"><input type="number" min="1" max="730" data-num="'+esc(k)+'" value="'+esc(editorLens[k]??'')+'"><span class="sub">days</span></div></div>';return''}).join('');for(const b of controls.querySelectorAll('[data-bool]'))b.onclick=()=>{const k=b.dataset.bool,v=editorLens[k];if(v===undefined)editorLens[k]=true;else if(v===true)editorLens[k]=false;else delete editorLens[k];renderLensControls(dims)};for(const b of controls.querySelectorAll('[data-enum]'))b.onclick=()=>{const k=b.dataset.enum,v=b.dataset.val;const a=new Set(arr(editorLens[k]));a.has(v)?a.delete(v):a.add(v);if(a.size)editorLens[k]=[...a];else delete editorLens[k];renderLensControls(dims)};for(const i of controls.querySelectorAll('[data-num]'))i.onchange=()=>{const k=i.dataset.num,v=Number(i.value);if(Number.isInteger(v)&&v>=1&&v<=730)editorLens[k]=v;else delete editorLens[k]};document.getElementById('applyLens').onclick=()=>go('scout_apply_opportunity_lens',{candidate_keys:arr(state.candidate_keys),lens:editorLens,limit:25})}
function filteredEvents(){const ev=arr(state.events);if(timelineFilter==='all')return ev;if(timelineFilter==='future')return ev.filter(e=>e.temporal_role==='future');return ev.filter(e=>e.evidence_class===timelineFilter)}
function renderTimeline(){const current=obj(state.current_opportunity),target=obj(current.target),sig=obj(current.signal),events=filteredEvents();if(selected>=events.length)selected=Math.max(0,events.length-1);const hist=events.filter(e=>e.temporal_role!=='future'),future=events.filter(e=>e.temporal_role==='future');const nodes=a=>a.map(e=>{const global=events.indexOf(e),cls=e.evidence_class||'fact',futureCls=e.temporal_role==='future'?' future':'';return '<div class="eventNode '+esc(cls)+futureCls+(global===selected?' selected':'')+'"><button data-event="'+global+'" aria-label="'+esc(e.title||label(e.kind))+'"><span>'+esc(cls==='counter_evidence'?'!':cls==='inference'?'I':cls==='decision'?'D':'F')+'</span></button><small>'+esc(date(e.occurred_at))+'</small></div>'}).join('');const e=events[selected];root.innerHTML='<section class="panel"><div class="head">'+backButton()+'<div class="kicker">Evidence chronology</div><h1>'+esc(current.display_name||target.name||state.candidate_key)+'</h1><div class="sub">'+esc(sig.why_now||'Scout is preserving the known chronology around this opportunity.')+'</div><div class="chips">'+[['all','All'],['fact','Facts'],['inference','Inference'],['counter_evidence','Counter'],['decision','Decisions'],['future','Future']].map(([k,t])=>'<button class="chip '+(timelineFilter===k?'on':'')+'" data-tfilter="'+k+'">'+t+'</button>').join('')+'</div></div><div class="section"><div class="railControls"><div><b>Earlier</b></div><div class="right"><button id="prevEvent" class="railBtn" aria-label="Previous event">←</button><button id="nextEvent" class="railBtn" aria-label="Next event">→</button></div></div><div id="rail" class="railViewport"><div class="railTrack">'+nodes(hist)+'<div class="nowNode"><div class="nowMark"></div><small>NOW</small></div>'+nodes(future)+'</div></div>'+(e?'<div class="eventDetail"><div class="eventMeta"><span class="tag '+esc(e.evidence_class||'fact')+'">'+esc(label(e.evidence_class||'fact'))+'</span><span class="tag">'+esc(label(e.temporal_role||'historical'))+'</span>'+(e.source_kind?'<span class="tag">'+esc(label(e.source_kind))+'</span>':'')+'</div><b>'+esc(e.title||label(e.kind))+'</b><div class="why">'+esc(e.summary||detailSummary(e.detail)||'No additional narrative was recorded for this event.')+'</div><div class="sub">'+esc(date(e.occurred_at))+(e.confidence!==undefined?' · confidence '+esc(Math.round(Number(e.confidence)*100))+'%':'')+'</div></div>':'<div class="empty"><b>No events in this view</b><span class="sub">The filter is hiding the known chronology; Scout will not fill gaps with invented history.</span></div>')+'</div></section>';wireBack();for(const b of document.querySelectorAll('[data-tfilter]'))b.onclick=()=>{timelineFilter=b.dataset.tfilter;selected=0;renderTimeline()};for(const b of document.querySelectorAll('[data-event]'))b.onclick=()=>{selected=Number(b.dataset.event||0);renderTimeline()};const step=d=>{selected=Math.max(0,Math.min(events.length-1,selected+d));renderTimeline()};document.getElementById('prevEvent').onclick=()=>step(-1);document.getElementById('nextEvent').onclick=()=>step(1)}
function detailSummary(d){const o=obj(d),parts=[];for(const [k,v] of Object.entries(o).slice(0,3)){if(v===null||v===undefined||typeof v==='object')continue;parts.push(label(k)+': '+String(v))}return parts.join(' · ')}
function graphSvg(nodes,edges){const n=nodes.length,w=640,h=360,cx=w/2,cy=h/2,r=n<=4?115:135;const pts=nodes.map((_,i)=>{const a=-Math.PI/2+i*2*Math.PI/n;return{x:cx+r*Math.cos(a),y:cy+r*Math.sin(a)}});const index=new Map(nodes.map((x,i)=>[x.candidate_key,i]));const lines=edges.map(e=>{const a=index.get(e.from_candidate_key),b=index.get(e.to_candidate_key);if(a===undefined||b===undefined)return'';return '<line class="edge '+esc(e.strength||'supporting')+'" x1="'+pts[a].x+'" y1="'+pts[a].y+'" x2="'+pts[b].x+'" y2="'+pts[b].y+'" />'}).join('');const circles=nodes.map((x,i)=>'<circle class="node '+(i===selected?'selected':'')+'" cx="'+pts[i].x+'" cy="'+pts[i].y+'" r="24"/><text class="nodeLabel '+(i===selected?'selected':'')+'" x="'+pts[i].x+'" y="'+pts[i].y+'">'+(i+1)+'</text>').join('');return '<svg class="graph" viewBox="0 0 '+w+' '+h+'" role="img" aria-label="Relationship diagram for '+nodes.length+' surfaced opportunities">'+lines+circles+'</svg>'}
function renderConstellation(){const nodes=arr(state.nodes),edges=arr(state.edges);if(selected>=nodes.length)selected=0;const n=nodes[selected]||{},o=obj(n.opportunity),sig=obj(o.signal),buyer=obj(o.buyer),scale=obj(o.commercial_scale),target=obj(o.target);root.innerHTML='<section class="panel"><div class="head">'+backButton()+'<div class="kicker">Known-node relationships</div><h1>Opportunity Constellation</h1><div class="sub">'+esc(nodes.length)+' surfaced opportunities · '+esc(edges.length)+' explainable relationships. No hidden nodes are added.</div></div><div class="section graphWrap">'+graphSvg(nodes,edges)+'<div class="nodeList" aria-label="Opportunity nodes">'+nodes.map((x,i)=>'<button class="nodeBtn '+(i===selected?'on':'')+'" data-node="'+i+'">'+(i+1)+' · '+esc(obj(x.opportunity).display_name||obj(obj(x.opportunity).target).name||'Opportunity')+'</button>').join('')+'</div></div>'+(nodes.length?'<div class="section"><div class="sectionTitle"><b>Selected node</b><span>#'+(selected+1)+'</span></div><h1 style="font-size:20px">'+esc(o.display_name||target.name||n.candidate_key)+'</h1><div class="why">'+esc(sig.why_now||'Scout has current evidence worth investigating.')+'</div><div class="summaryStrip" style="padding:0">'+metric('Buyer',buyer.organization_name||label(buyer.resolution_status||'unresolved'))+metric('Scale',label(scale.status||'unknown'))+metric('Service',label(o.primary_service_slug||arr(o.services)[0]||'opportunity'))+'</div><div class="toolbar"><button id="nodeTime" class="btn primary">Time Machine</button></div></div>':'')+'<div class="section"><div class="sectionTitle"><b>Relationships</b><span>Categorical, explainable</span></div><div class="edgeList">'+(edges.length?edges.map(e=>{const a=nodes.findIndex(n=>n.candidate_key===e.from_candidate_key)+1,b=nodes.findIndex(n=>n.candidate_key===e.to_candidate_key)+1;return '<div class="edgeCard"><b>'+a+' ↔ '+b+' · '+esc(label(e.relation))+'</b><br>'+esc(e.explanation||'Related')+' · '+esc(label(e.strength||'supporting'))+'</div>'}).join(''):'<div class="empty"><b>No explainable edges</b><span class="sub">These surfaced candidates do not currently form a documented constellation under the selected relationship rules.</span></div>')+'</div></div></section>';wireBack();for(const b of document.querySelectorAll('[data-node]'))b.onclick=()=>{selected=Number(b.dataset.node||0);renderConstellation()};const t=document.getElementById('nodeTime');if(t)t.onclick=()=>go('scout_get_opportunity_timeline',{candidate_key:n.candidate_key,include_future:true,limit:100})}
app.ontoolresult=r=>{state=norm(r);history=[];selected=0;timelineFilter='all';render()};await app.connect();
</script></body></html>`

function makeServer(connection:Connection){
  const server=new McpServer({name:'Scout by Cadastory Exploration',version:'1.1.0',description:'Interactive known-candidate exploration for Scout Lens, Evidence Time Machine, and Opportunity Constellations.'},{instructions:'These tools operate only on exact Scout candidate keys already surfaced to the connected organization. Scout Lens narrows known candidates and never expands discovery. Evidence Time Machine is candidate-scoped and preserves fact/inference/counter-evidence/decision distinctions. Opportunity Constellations relate only supplied known nodes; never infer or add hidden nodes. Never expose proprietary ranking formulas or use these tools to probe blocked opportunity sets.'})
  const present=async(toolName:string,data:unknown)=>await rpc('scout_decorate_agent_response_v2',{p_connection_id:connection.connection_id,p_tool_name:toolName,p_payload:data??null})
  const wrap=async(toolName:string,text:string,fn:()=>Promise<unknown>)=>{try{return result(await present(toolName,await fn()),text)}catch(e){return errorResult(e)}}
  registerAppTool(server,'scout_get_opportunity_lens_catalog',{title:'Get Scout Lens Catalog',description:'Read the safe semantic dimensions Scout Lens can apply to an already surfaced candidate set. This does not search for opportunities.',inputSchema:{},annotations:{readOnlyHint:true,destructiveHint:false,idempotentHint:true,openWorldHint:false},_meta:{ui:{resourceUri:LENS_URI}}},async()=>wrap('scout_get_opportunity_lens_catalog','Scout Lens semantic filter catalog.',async()=>{requireScope(connection,'profile:read');requireScope(connection,'opportunities:read');return addUi(await rpc('scout_get_connection_opportunity_lens_catalog_v1',{p_connection_id:connection.connection_id}),'lens_catalog')}))
  registerAppTool(server,'scout_apply_opportunity_lens',{title:'Apply Scout Lens',description:'Narrow 1-25 exact candidate keys already returned by Scout using typed buyer, contact, timing, recurrence, scale, access, freshness, event, or signal dimensions. Never use for initial discovery.',inputSchema:{candidate_keys:z.array(candidateKey).min(1).max(25),lens,limit:z.number().int().min(1).max(25).optional()},annotations:{readOnlyHint:true,destructiveHint:false,idempotentHint:true,openWorldHint:false},_meta:{ui:{resourceUri:LENS_URI}}},async({candidate_keys,lens,limit})=>wrap('scout_apply_opportunity_lens','Scout Lens applied to the supplied known candidates.',async()=>{requireScope(connection,'profile:read');requireScope(connection,'opportunities:read');return addUi(await rpc('scout_apply_connection_opportunity_lens_v1',{p_connection_id:connection.connection_id,p_candidate_keys:candidate_keys,p_lens:lens,p_limit:limit??25}),'lens',{candidate_keys})}))
  registerAppTool(server,'scout_get_opportunity_timeline',{title:'Get Opportunity Evidence Timeline',description:'Read the Evidence Time Machine chronology for one exact candidate already surfaced by Scout, including typed evidence events and optional future milestones. Never invent missing history.',inputSchema:{candidate_key:candidateKey,include_future:z.boolean().optional(),limit:z.number().int().min(1).max(200).optional()},annotations:{readOnlyHint:true,destructiveHint:false,idempotentHint:true,openWorldHint:false},_meta:{ui:{resourceUri:TIME_URI}}},async({candidate_key,include_future,limit})=>wrap('scout_get_opportunity_timeline','Evidence Time Machine chronology for the supplied known candidate.',async()=>{requireScope(connection,'profile:read');requireScope(connection,'opportunities:read');return addUi(await rpc('scout_get_connection_opportunity_timeline_v1',{p_connection_id:connection.connection_id,p_candidate_key:candidate_key,p_include_future:include_future??true,p_limit:limit??100}),'time_machine')}))
  registerAppTool(server,'scout_get_opportunity_constellation',{title:'Get Opportunity Constellation',description:'Relate 2-12 exact candidates already surfaced by Scout using explainable shared-buyer, same-asset/target, shared-event, explicit-derivation, and bounded geographic-proximity edges. Never add hidden nodes.',inputSchema:{candidate_keys:z.array(candidateKey).min(2).max(12),max_proximity_miles:z.number().min(0.1).max(100).optional()},annotations:{readOnlyHint:true,destructiveHint:false,idempotentHint:true,openWorldHint:false},_meta:{ui:{resourceUri:CONSTELLATION_URI}}},async({candidate_keys,max_proximity_miles})=>wrap('scout_get_opportunity_constellation','Opportunity Constellation for the supplied known candidates.',async()=>{requireScope(connection,'profile:read');requireScope(connection,'opportunities:read');return addUi(await rpc('scout_get_connection_opportunity_constellation_v1',{p_connection_id:connection.connection_id,p_candidate_keys:candidate_keys,p_max_proximity_miles:max_proximity_miles??25}),'constellations',{candidate_keys})}))
  const resource=(uri:string)=>({contents:[{uri,mimeType:RESOURCE_MIME_TYPE,text:APP_HTML,_meta:{ui:{prefersBorder:false,csp:{connectDomains:[],resourceDomains:['https://esm.sh']}}}}]})
  registerAppResource(server,'scout-lens-app',LENS_URI,{mimeType:RESOURCE_MIME_TYPE},async()=>resource(LENS_URI))
  registerAppResource(server,'scout-evidence-time-machine-app',TIME_URI,{mimeType:RESOURCE_MIME_TYPE},async()=>resource(TIME_URI))
  registerAppResource(server,'scout-constellations-app',CONSTELLATION_URI,{mimeType:RESOURCE_MIME_TYPE},async()=>resource(CONSTELLATION_URI))
  return server
}
function headers(base?:HeadersInit){const h=new Headers(base);h.set('access-control-allow-origin','*');h.set('access-control-allow-headers','authorization,content-type,accept,mcp-protocol-version,mcp-session-id,x-request-id');h.set('access-control-allow-methods','GET,POST,DELETE,OPTIONS');h.set('cache-control','no-store, max-age=0');h.set('pragma','no-cache');h.set('x-content-type-options','nosniff');h.set('referrer-policy','no-referrer');h.set('x-scout-contract',CONTRACT_VERSION);h.set('x-scout-exposure-contract',EXPOSURE_CONTRACT_VERSION);h.set('x-scout-enumeration-contract',ENUMERATION_CONTRACT_VERSION);return h}
Deno.serve(async(req:Request)=>{if(req.method==='OPTIONS')return new Response(null,{status:204,headers:headers()});const connection=await authenticate(req);if(!connection)return Response.json({error:'invalid or expired Scout connection'},{status:401,headers:headers()});try{const server=makeServer(connection);const transport=new WebStandardStreamableHTTPServerTransport();await server.connect(transport);const response=await transport.handleRequest(req);return new Response(response.body,{status:response.status,statusText:response.statusText,headers:headers(response.headers)})}catch(e){console.error('Scout exploration MCP error',e);return Response.json({error:normalizeToolError(e)},{status:500,headers:headers()})}})
